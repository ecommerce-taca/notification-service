// reference.type cho deep-link (allowlist API §3.1)
export enum ReferenceType {
  ORDER = 'ORDER',
  SHIPMENT = 'SHIPMENT',
  PAYMENT = 'PAYMENT',
  REVIEW = 'REVIEW',
  CONVERSATION = 'CONVERSATION',
  SHOP = 'SHOP',
}
